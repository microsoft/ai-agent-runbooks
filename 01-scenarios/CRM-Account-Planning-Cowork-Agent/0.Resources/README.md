# Resources — CRM Account Planning Cowork Agent

Place scenario assets here:

| Folder / file | Purpose |
|---|---|
| `Images/` | Screenshots referenced from `3.Runbook.md` |
| `MCP-Server/` | Tool design table, plugin manifest, and the `whoami` identity spike |
| `Skills/` | Version-controlled custom skill definitions (account plan, QBR, pipeline review) |
| `Templates/` | Reference `.potx`, `.dotx`, and `.xltx` used for brand compliance testing |
| `Evaluation/` | The 20-account evaluation set and four-axis scoring results |
| `Compliance/` | The compliance position table and any documented gaps |

> 📌 The tool design table and the custom skills are the two artifacts worth reusing on the next
> engagement. The MCP server code will differ per CRM; the tool *shapes* rarely do.
