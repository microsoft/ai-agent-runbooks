# Resources — Employee Self-Service Agent

Place scenario assets here:

| Folder / file | Purpose |
|---|---|
| `Images/` | Screenshots referenced from `3.Runbook.md` |
| `Content-audit/` | The content architecture table and remediation tracker |
| `Evaluation/` | Deflection baseline, evaluation set, pilot results |
| `Instructions/` | Version-controlled agent instruction blocks |

> 📌 The deflection baseline (top 25 question categories with volumes, captured **before**
> deployment) is the most valuable artefact in this folder. Without it there is no way to prove
> the agent worked.
