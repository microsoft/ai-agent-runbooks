# F&O User Onboarding Agent — Architecture

## 1. Logical Architecture

The F&O User Onboarding Agent operates across three layers: the **User Layer** (how requests
are submitted and approvals are captured), the **Agent Layer** (how the agent orchestrates the
workflow), and the **Integration & Data Layer** (the systems the agent interacts with to
provision users end-to-end).

> *Architecture overview — all components and connections*
>
> <img src="0.Resources/Images/Diagram001.png" alt="F&O User Onboarding Agent - Architecture Overview" width="1200">

### How It Works

```mermaid
flowchart TB
    subgraph USER["1. User Layer"]
        direction LR
        U1["HR Admin / IT Admin<br/>(M365 Copilot / Teams)<br/>Submits onboarding request"]
        U2["Manager / Approver<br/>(Microsoft Teams)<br/>Reviews Adaptive Card approval"]
    end

    subgraph AGENT["2. Agent Layer — Copilot Studio"]
        direction LR
        IN["Agent Instructions<br/>─────────────────<br/>System Prompt<br/>Mode Control Logic"]
        AG["FOUserOnboardingAgent<br/>─────────────────<br/>Orchestrator: GPT-4.1<br/>Web Search: Disabled"]
        TP["Conversational Topics<br/>─────────────────<br/>Onboarding Form<br/>Approval Routing<br/>Provisioning Status"]
    end

    subgraph DATA["3. Integration and Data Layer"]
        direction LR
        DV["Dataverse<br/>─────────────────<br/>OnboardAgentConfig<br/>· CreateADUser flag<br/>· Approval flag<br/>· Graph API credentials"]
        GR["Microsoft Graph API<br/>─────────────────<br/>Entra ID<br/>· Validate user<br/>· Create user"]
        PA["Power Automate<br/>─────────────────<br/>Orchestration Flows<br/>· Approval routing<br/>· D365 provisioning"]
        FO["Dynamics 365 F&O<br/>─────────────────<br/>· Company setup<br/>· Security roles<br/>· User activation"]
    end

    USER --> AGENT
    AGENT --> DATA

    U1 -->|"Submit onboarding form"| AG
    IN -->|"Controls mode behavior"| AG
    AG -->|"Routes to topics"| TP
    TP -->|"Read config"| DV
    DV -->|"Config values"| PA
    PA -->|"Validate / Create user"| GR
    GR -->|"User status"| PA
    PA -->|"Send Adaptive Card"| U2
    U2 -->|"Approve / Reject"| PA
    PA -->|"Provision user"| FO
    FO -->|"Provisioning result"| AG
    AG -->|"Status response"| U1

    style U1 fill:#0078D4,color:#fff,stroke:#005A9E
    style U2 fill:#0078D4,color:#fff,stroke:#005A9E
    style AG fill:#107C10,color:#fff,stroke:#004B00
    style TP fill:#107C10,color:#fff,stroke:#004B00
    style IN fill:#107C10,color:#fff,stroke:#004B00
    style DV fill:#E07000,color:#fff,stroke:#A05000
    style GR fill:#E07000,color:#fff,stroke:#A05000
    style PA fill:#E07000,color:#fff,stroke:#A05000
    style FO fill:#E07000,color:#fff,stroke:#A05000
    style USER fill:#fffde7,stroke:#aaa
    style AGENT fill:#fffde7,stroke:#aaa
    style DATA fill:#fffde7,stroke:#aaa
```

---

## 2. Key Components

| Component | Technology | Role |
|---|---|---|
| **Agent Runtime** | Microsoft Copilot Studio | Core agent orchestration, conversation management, and topic routing |
| **LLM / Orchestrator** | GPT-4.1 (Copilot Studio) | Natural language understanding and form-guided conversation |
| **Agent Configuration** | Dataverse — `OnboardAgentConfig` table | Stores all operational flags: CreateADUser, approval settings, Graph API credentials, Teams channel configuration |
| **Entra ID Integration** | Microsoft Graph API (HTTP connector) | Validates user existence and creates new Entra ID accounts when `CreateADUser = 1` |
| **Approval Workflow** | Power Automate + Teams Adaptive Card | Routes provisioning request to manager/admin via Teams channel (Mode 1 only); waits for approve/reject response |
| **D365 F&O Provisioning Flow** | Power Automate Cloud Flow | Provisions user in the target D365 F&O company and assigns the configured security roles |
| **Orchestration Flow** | Power Automate Cloud Flow | Main flow coordinating Entra ID check → optional creation → optional approval → D365 F&O provisioning |
| **Agent Environment** | Power Platform Environment (Dataverse-enabled) | Hosts agent artifacts, Dataverse tables, and Power Automate flows |
| **Deployment Channels** | Microsoft Teams + M365 Copilot | Primary channels for HR/IT admins to submit onboarding requests |

---

## 3. Data Flow

> *Power Automate flow — Mode 1 (Create + Teams Approval + Provision)*
>
> <img src="0.Resources/Images/Diagram002.png" alt="Mode 1 Power Automate Flow" width="900">

> *Power Automate flow — Mode 2 (Validation Only)*
>
> <img src="0.Resources/Images/Diagram003.png" alt="Mode 2 Validation Flow" width="900">

> *Power Automate flow — Mode 3 (Direct Auto-Provisioning)*
>
> <img src="0.Resources/Images/Diagram004.png" alt="Mode 3 Direct Provisioning Flow" width="900">

### Mode 1 — Auto-Create + Approval Required

This is the most governed mode. Entra ID account is created automatically, and a Teams
Adaptive Card is sent for manager approval before D365 F&O access is granted.

```mermaid
sequenceDiagram
    actor Admin as HR / IT Admin
    participant CS as Copilot Studio
    participant PA as Power Automate
    participant GR as Microsoft Graph API
    participant TM as Teams Channel
    actor Approver as Manager / Admin
    participant FO as D365 F&O

    Admin->>CS: Submits onboarding request (name, email, company, roles)
    CS->>PA: Triggers main orchestration flow
    PA->>GR: GET /users — check if user exists in Entra ID
    GR-->>PA: User not found (404)
    PA->>GR: POST /users — create Entra ID user account
    GR-->>PA: User created (200 · isusercreated = 1)
    PA->>TM: Send Adaptive Card approval request
    TM-->>Approver: Adaptive Card displayed in Teams channel
    Approver-->>TM: Clicks Approve
    TM-->>PA: Approval response received
    PA->>FO: Provision user — company setup + security role assignment
    FO-->>PA: Provisioning success
    PA-->>CS: Return success status
    CS-->>Admin: Confirmation message with user details
```

---

### Mode 2 — Validate Only (No Auto-Create)

In this mode the agent acts as a **gating check**. It validates that the user already
exists in Entra ID before allowing D365 F&O provisioning to proceed. If the user is not
found, the flow stops with an actionable error message.

```mermaid
sequenceDiagram
    actor Admin as HR / IT Admin
    participant CS as Copilot Studio
    participant PA as Power Automate
    participant GR as Microsoft Graph API
    participant FO as D365 F&O

    Admin->>CS: Submits onboarding request
    CS->>PA: Triggers orchestration flow (CreateADUser = 0)
    PA->>GR: GET /users — check if user exists in Entra ID

    alt Path A — User exists
        GR-->>PA: User found (200)
        PA->>FO: Provision user — company setup + security role assignment
        FO-->>PA: Provisioning success
        PA-->>CS: Return success status
        CS-->>Admin: Confirmation message
    else Path B — User not found
        GR-->>PA: User not found (404)
        PA-->>CS: Error — "AD User not found. Cannot proceed."
        CS-->>Admin: Error message with next-step guidance
    end
```

---

### Mode 3 — Auto-Create + Direct Provisioning (No Approval)

This is the streamlined mode for environments where pre-authorization exists. Entra ID
account is created automatically (if not present) and the user is provisioned in D365 F&O
directly, without a Teams approval step.

```mermaid
sequenceDiagram
    actor Admin as HR / IT Admin
    participant CS as Copilot Studio
    participant PA as Power Automate
    participant GR as Microsoft Graph API
    participant FO as D365 F&O

    Admin->>CS: Submits onboarding request
    CS->>PA: Triggers orchestration flow (CreateADUser = 1, Approval = No)
    PA->>GR: GET /users — check if user exists in Entra ID

    alt Path A — User already exists
        GR-->>PA: User found — skip creation (shows notice to requestor)
        PA->>FO: Provision user — company setup + security role assignment
        FO-->>PA: Provisioning success
        PA-->>CS: Return success status
        CS-->>Admin: Confirmation + skip notice
    else Path B — User does not exist
        GR-->>PA: User not found (404)
        PA->>GR: POST /users — create Entra ID user
        GR-->>PA: "AD User Created Successfully"
        PA->>FO: Provision user — company setup + security role assignment
        FO-->>PA: Provisioning success
        PA-->>CS: Return success status
        CS-->>Admin: Confirmation message with new user details
    end
```

---

## 4. Configuration Architecture

> *OnboardAgentConfig table and operational mode selection matrix*
>
> <img src="0.Resources/Images/Diagram006.png" alt="OnboardAgentConfig and Mode Selection" width="1200">

All agent behavior is controlled by the **`OnboardAgentConfig`** table in Dataverse.
There is no need to modify agent code or flows when switching operational modes.

```mermaid
flowchart LR
    subgraph CONFIG["OnboardAgentConfig (Dataverse)"]
        direction TB
        F1["CreateADUser<br/>(0 = Validate only · 1 = Auto-create)"]
        F2["ApprovalRequired<br/>(Yes · No)"]
        F3["TeamName<br/>Target Teams team for approvals"]
        F4["ChannelName<br/>Target channel (e.g. FNOUserApprove)"]
        F5["GraphClientID<br/>App Registration Client ID"]
        F6["GraphClientSecret<br/>App Registration Client Secret"]
        F7["GraphTenantID<br/>Azure AD Tenant ID"]
    end

    subgraph MODES["Operational Mode Selection"]
        direction TB
        M1["Mode 1<br/>CreateADUser=1 · Approval=Yes"]
        M2["Mode 2<br/>CreateADUser=0 · Approval=N/A"]
        M3["Mode 3<br/>CreateADUser=1 · Approval=No"]
    end

    CONFIG --> MODES

    style F1 fill:#E07000,color:#fff,stroke:#A05000
    style F2 fill:#E07000,color:#fff,stroke:#A05000
    style F3 fill:#0078D4,color:#fff,stroke:#005A9E
    style F4 fill:#0078D4,color:#fff,stroke:#005A9E
    style F5 fill:#5C2D91,color:#fff,stroke:#3B1A6B
    style F6 fill:#5C2D91,color:#fff,stroke:#3B1A6B
    style F7 fill:#5C2D91,color:#fff,stroke:#3B1A6B
    style M1 fill:#107C10,color:#fff,stroke:#004B00
    style M2 fill:#107C10,color:#fff,stroke:#004B00
    style M3 fill:#107C10,color:#fff,stroke:#004B00
```

### Required Graph API Permissions by Mode

| Mode | Permissions Required | Scope |
|---|---|---|
| **Mode 2** (Validate Only) | `User.Read.All` | Read-only — check user existence |
| **Mode 1** (Auto-Create + Approval) | `User.ReadWrite.All` · `Directory.ReadWrite.All` | Create user accounts and assign directory roles |
| **Mode 3** (Auto-Create, No Approval) | `User.ReadWrite.All` · `Directory.ReadWrite.All` | Create user accounts and assign directory roles |

---

## 5. Agent Conversation & Topic Flow

> *Copilot Studio conversation flow — topics, form collection, and action routing*
>
> <img src="0.Resources/Images/Diagram005.png" alt="Agent Conversation and Topic Flow" width="1100">

---

## 6. Power Automate Flow Architecture

The agent delegates all integration work to Power Automate flows. The three core flows
are described below:

```mermaid
flowchart TB
    subgraph FLOWS["Power Automate Flows"]
        direction TB
        F1["Main Orchestration Flow<br/>─────────────────<br/>1. Read OnboardAgentConfig<br/>2. Call Graph API (validate/create user)<br/>3. Route to Approval or Direct Provision<br/>4. Return result to agent"]

        F2["Teams Approval Flow<br/>─────────────────<br/>1. Build Adaptive Card payload<br/>2. Post to Teams channel (FNOUserApprove)<br/>3. Wait for approval response<br/>4. Return Approved / Rejected to orchestrator"]

        F3["D365 F&O Provision Flow<br/>─────────────────<br/>1. Receive user details + company + roles<br/>2. Create user in D365 F&O<br/>3. Assign security roles<br/>4. Activate user account<br/>5. Return provisioning status"]
    end

    F1 -->|"Mode 1: Route to approval"| F2
    F2 -->|"Approved"| F3
    F1 -->|"Mode 2/3: Direct provision"| F3

    style F1 fill:#107C10,color:#fff,stroke:#004B00
    style F2 fill:#5C2D91,color:#fff,stroke:#3B1A6B
    style F3 fill:#E07000,color:#fff,stroke:#A05000
```

---

## 7. Security & Governance Considerations

| Area | Consideration |
|---|---|
| **Graph API Credentials** | Client ID, Client Secret, and Tenant ID stored in the `OnboardAgentConfig` Dataverse table; credentials are not hardcoded in flows |
| **Least-Privilege Permissions** | Mode 2 requires only `User.Read.All`; Modes 1 and 3 require `User.ReadWrite.All` and `Directory.ReadWrite.All` — use the minimum permissions for the environment's operational mode |
| **Approval Chain** | Mode 1 enforces explicit manager/admin approval via Teams Adaptive Card before any D365 F&O access is granted — creates an auditable approval event |
| **Data Scope** | Agent does not use web search or general LLM knowledge — all provisioning actions are driven by the structured form input and configuration table |
| **Connection Credentials** | Power Automate connections (Teams, D365 F&O, Dataverse) use the maker's service account or managed identity — review before production deployment |
| **Access Control** | D365 F&O security role assignment is scoped to the roles defined in the onboarding form — no blanket admin access is granted |
| **Dataverse Security** | `OnboardAgentConfig` table should be restricted to Power Platform admins; Graph API credentials in the table must be treated as sensitive secrets |

---

## Related Resources

| Resource | Link |
|---|---|
| Scenario Overview | [1.Overview.md](./1.Overview.md) |
| Step-by-Step Runbook | [3.Runbook.md](./3.Runbook.md) |
| Sample Prompts | [4.Sample-prompts.md](./4.Sample-prompts.md) |
| UAT Test Guide | [5.UAT-Test-Guide.md](./5.UAT-Test-Guide.md) |

---
