# Step-by-Step Runbook — F&O User Onboarding Agent

> **Scenario**: Automated User Provisioning for Dynamics 365 Finance & Operations
> **Platform**: Microsoft Copilot Studio · Power Automate · Microsoft Graph API · Dataverse · D365 F&O
> **Target Readers**: Delivery engineer, Power Platform admin, IT admin
> **Last Updated**: April 2026

---

## Overview

This runbook walks you through the complete setup and deployment of the **F&O User Onboarding Agent**.
By the end of this guide, the agent will be able to:

- Accept a structured onboarding request from an HR or IT admin via Microsoft Teams / M365 Copilot
- Validate or automatically create the user in Microsoft Entra ID using the Graph API
- Route the request for manager/admin approval via a Teams Adaptive Card (Mode 1 only)
- Provision the user in the correct D365 F&O company with the appropriate security roles
- Return a success or failure notification to the requestor

### Administration Portals Used in This Runbook

| Portal | URL | Purpose |
|---|---|---|
| Azure Portal | https://portal.azure.com | App registration for Graph API access |
| Microsoft 365 Admin Center | https://admin.microsoft.com | License and user management |
| Power Platform Admin Center | https://admin.powerplatform.microsoft.com | Environment management |
| Power Apps Maker Portal | https://make.powerapps.com | Solution import, Dataverse configuration |
| Copilot Studio | https://copilotstudio.microsoft.com | Agent customization, testing, publishing |
| Teams Admin Center | https://admin.teams.microsoft.com | Custom app sideloading settings |
| D365 F&O | Your environment URL | Security role and user verification |

---

## Prerequisites

Before starting, confirm the following are in place:

### Environment Requirements

| Requirement | Details | Where to Configure |
|---|---|---|
| Power Platform Environment | Dataverse-enabled, non-default environment | Power Platform Admin Center |
| Dynamics 365 F&O Environment | Active F&O environment accessible by the provisioning service account | D365 F&O Admin |
| Azure Subscription | Required for App Registration (Graph API) | Azure Portal |
| Microsoft 365 Tenant | Same tenant as D365 F&O and Power Platform | M365 Admin Center |

### License Requirements

| License | Who Needs It | Purpose |
|---|---|---|
| Copilot Studio User License | Delivery engineer / agent author | Build and publish the agent |
| Microsoft 365 E3 / E5 | End users (HR/IT Admins) | Access agent via Teams or M365 Copilot |
| Microsoft 365 Copilot License | End users (if using M365 Copilot channel) | M365 Copilot agent interaction |
| D365 F&O License | New hire being provisioned | Required for D365 F&O access |

### Access and Permissions

| Role | Where | Purpose |
|---|---|---|
| Global Admin or Application Admin | Azure AD / Entra ID | Grant API permissions to App Registration |
| Power Platform Administrator | Power Platform Admin Center | Create environments, import solutions |
| System Administrator (D365 F&O) | D365 F&O | Create users and assign security roles |
| Teams Admin | Teams Admin Center | Enable custom app sideloading |

---

## Phase 0: Azure App Registration (Graph API Access)

This phase creates the Azure App Registration that allows the agent's Power Automate
flows to authenticate against the Microsoft Graph API for Entra ID operations.

### Step 0-1. Create the App Registration

1. Sign in to the **Azure Portal** → https://portal.azure.com
2. Navigate to **Microsoft Entra ID** → **App registrations** → **+ New registration**
3. Fill in the following values:

   | Field | Value |
   |---|---|
   | Name | `FOUserOnboardingAgent` (or your preferred name) |
   | Supported account types | Accounts in this organizational directory only (Single tenant) |
   | Redirect URI | Leave blank |

4. Click **Register**
5. On the app's overview page, copy and save the following values — you will need them later:
   - **Application (client) ID**
   - **Directory (tenant) ID**

---

### Step 0-2. Assign API Permissions

The permissions required depend on the operational mode you are deploying:

> 💡 Start with the **minimum required permissions** for your environment's mode.
> You can always add permissions later if you switch modes.

**For Mode 2 (Validate Only):**

1. In the app registration, go to **API permissions** → **+ Add a permission**
2. Select **Microsoft Graph** → **Application permissions**
3. Search and add: `User.Read.All`
4. Click **Add permissions**
5. Click **Grant admin consent for [your tenant]** → **Yes**

**For Mode 1 or Mode 3 (Auto-Create User):**

1. In the app registration, go to **API permissions** → **+ Add a permission**
2. Select **Microsoft Graph** → **Application permissions**
3. Search and add each of the following:
   - `User.ReadWrite.All`
   - `Directory.ReadWrite.All`
4. Click **Add permissions**
5. Click **Grant admin consent for [your tenant]** → **Yes**

> ⚠️ **Admin consent is required.** Without it, the API calls will return 403 Forbidden.
> The user performing this step must be a **Global Administrator** or **Application Administrator**.

---

### Step 0-3. Create a Client Secret

1. In the app registration, go to **Certificates & secrets** → **+ New client secret**
2. Fill in:

   | Field | Value |
   |---|---|
   | Description | `FOUserOnboardingAgent-Secret` |
   | Expires | 12 months (or per your org's policy) |

3. Click **Add**
4. **Copy the secret value immediately** — it will not be shown again after you leave this page
5. Store the secret securely; you will add it to Dataverse in Phase 2

---

## Phase 1: Power Platform Environment Setup

### Step 1-1. Verify or Create the Power Platform Environment

1. Go to **Power Platform Admin Center** → https://admin.powerplatform.microsoft.com
2. Click **Environments** → confirm a Dataverse-enabled environment exists
3. If not, click **+ New** and fill in:

   | Field | Value |
   |---|---|
   | Name | `FO-Onboarding` (or preferred name) |
   | Type | Developer or Production |
   | Region | Same region as your D365 F&O environment |
   | Add a Dataverse datastore | ✅ Enabled |

4. Click **Save** and wait for environment provisioning to complete (5–10 minutes)

---

### Step 1-2. Enable Copilot Extensibility

1. Go to **Microsoft 365 Admin Center** → https://admin.microsoft.com
2. Navigate to **Integrated Apps** → click the **Settings icon** (top right)
3. Find **"Allow the following users access to Copilot agents"**
4. Confirm it is set to **All users** (or your specific group)

---

### Step 1-3. Enable Custom App Sideloading in Teams (if deploying to Teams)

1. Sign in to **Teams Admin Center** → https://admin.teams.microsoft.com
2. Go to **Teams apps** → **Manage apps** → **Actions** → **Org-wide app settings**
3. Under **Custom apps**, toggle **"Let users interact with custom apps in preview"** → **On** → **Save**
4. Go to **Teams apps** → **Setup policies** → **Global (Org-wide default)**
5. Toggle **"Upload custom apps"** → **On** → **Save**

---

### Step 1-4. Create the Teams Approval Team and Channel  ⚠️ Required before configuring OnboardAgentConfig

> **This step must be completed manually before populating the `OnboardAgentConfig` table.**
> The agent's Teams Approval Flow will post the Adaptive Card to the team and channel you
> configure here. If the team or channel does not exist, the approval flow will fail at runtime.

#### 1-4a. Create the Approval Team (if it does not already exist)

1. Open **Microsoft Teams** → click **Teams** in the left sidebar
2. Click **Join or create a team** → **Create team** → **From scratch**
3. Select **Private** (recommended — limit access to approvers only)
4. Set the team name to: **`F&O User Provisioning`**
5. Add the relevant approvers (managers / IT admins) as members
6. Click **Create**

> ✅ **This team already exists** in the target tenant:
>
> | Property | Value |
> |---|---|
> | **Team Name** | `F&O User Provisioning` |
> | **Group ID** | `28b69a3d-b7af-4f5b-be42-a53bd17dd6c4` |
> | **Tenant ID** | `1557f771-4c8e-4dbd-8b80-dd00a88e833e` |
> | **Team URL** | https://teams.microsoft.com/l/team/19%3A0oFu9NU3FTF6Dh1Y5CGCCOJx2yYl1XfCe5HAl-Gajko1%40thread.tacv2/conversations?groupId=28b69a3d-b7af-4f5b-be42-a53bd17dd6c4&tenantId=1557f771-4c8e-4dbd-8b80-dd00a88e833e |

---

#### 1-4b. Create the Approval Channel (if it does not already exist)

1. In the **F&O User Provisioning** team, click **···** (More options) next to the team name
2. Select **Add channel**
3. Set the channel name to: **`FNOUserApprove`**
4. Set type to **Standard**
5. Click **Add**

> ✅ **This channel already exists** in the target tenant:
>
> | Property | Value |
> |---|---|
> | **Channel Name** | `FNOUserApprove` |
> | **Channel ID** | `19%3A0oFu9NU3FTF6Dh1Y5CGCCOJx2yYl1XfCe5HAl-Gajko1%40thread.tacv2` |
> | **Channel URL** | https://teams.microsoft.com/l/channel/19%3A0oFu9NU3FTF6Dh1Y5CGCCOJx2yYl1XfCe5HAl-Gajko1%40thread.tacv2/FNOUserApprove?groupId=28b69a3d-b7af-4f5b-be42-a53bd17dd6c4&tenantId=1557f771-4c8e-4dbd-8b80-dd00a88e833e |

> 💡 **Tip**: Pin the `FNOUserApprove` channel so approvers see it immediately when they open Teams.
> Right-click the channel → **Pin**.

---

## Phase 2: Import and Configure the Solution

### Step 2-1. Import the Agent Solution

1. Go to **Power Apps Maker Portal** → https://make.powerapps.com
2. Switch to the correct environment (FO-Onboarding or equivalent)
3. Click **Solutions** → **Import solution**
4. Upload the solution file: `D365FOUserOnboardingAgent_1_0_0_2.zip`
5. Click **Next** and review the solution components
6. On the **Connections** step, create new connections for:
   - **Dataverse** (use the maker's account or service principal)
   - **Microsoft Teams** (use the approver's account or a service account)
   - **Dynamics 365 Finance & Operations** (use the provisioning service account)
   - **Office 365 Users** (if used in flows)
7. Click **Import** and wait for the import to complete

> ⚠️ If any flows fail to enable after import, go to **Solutions → F&O User Onboarding Agent**
> and manually enable each flow. Connection errors are the most common cause of import failures.

---

### Step 2-2. Configure the OnboardAgentConfig Table

The `OnboardAgentConfig` table in Dataverse is the **single configuration source** for the
agent. You must populate it before the agent can function.

1. In the **Power Apps Maker Portal**, go to **Dataverse** → **Tables**
2. Find and open the **OnboardAgentConfig** table
3. Click **Edit** to open the table data editor
4. Create a new row with the following values:

   | Column | Value | Notes |
   |---|---|---|
   | `CreateADUser` | `1` or `0` | `1` = auto-create Entra ID user; `0` = validate only |
   | `ApprovalRequired` | `Yes` or `No` | Only relevant when `CreateADUser = 1` |
   | `TeamName` | **`F&O User Provisioning`** | Must match the Teams team name exactly (case-sensitive) — see Step 1-4a |
   | `ChannelName` | **`FNOUserApprove`** | Must match the channel name exactly (case-sensitive) — see Step 1-4b |
   | `GraphClientID` | Application (client) ID from Step 0-1 | From Azure App Registration |
   | `GraphClientSecret` | Client secret value from Step 0-3 | Treat as sensitive — do not share |
   | `GraphTenantID` | Directory (tenant) ID from Step 0-1 | From Azure App Registration |

   > 📋 **Teams reference values for this deployment** (from Step 1-4):
   >
   > | Property | Value |
   > |---|---|
   > | Team Name | `F&O User Provisioning` |
   > | Channel Name | `FNOUserApprove` |
   > | Group ID | `28b69a3d-b7af-4f5b-be42-a53bd17dd6c4` |
   > | Channel ID | `19%3A0oFu9NU3FTF6Dh1Y5CGCCOJx2yYl1XfCe5HAl-Gajko1%40thread.tacv2` |
   > | Tenant ID | `1557f771-4c8e-4dbd-8b80-dd00a88e833e` |
   >
   > If your Power Automate flow uses **Group ID** and **Channel ID** directly (instead of
   > display names), use the values above in the corresponding flow action fields.

5. Click **Save**

> 🔒 **Security note**: The `OnboardAgentConfig` table contains Graph API credentials.
> Restrict table access to Power Platform admins only using table-level Dataverse security.

---

### Step 2-3. Set the Operational Mode

Based on your deployment requirement, set the correct flag combination:

| Mode | CreateADUser | ApprovalRequired | Use Case |
|---|---|---|---|
| **Mode 1** | `1` | `Yes` | Governed environments — requires formal approval before D365 F&O access |
| **Mode 2** | `0` | N/A | Pre-provisioned Entra ID — only validate before granting D365 F&O access |
| **Mode 3** | `1` | `No` | Pre-authorized environments — fully automated, no approval step |

---

### Step 2-4. Verify Power Automate Flows

1. In the **Power Apps Maker Portal**, go to **Solutions** → **F&O User Onboarding Agent** → **Cloud flows**
2. Confirm all flows are **On** (enabled):
   - `FO User Onboarding Flow` (main orchestration)
   - `Teams Approval Flow` (approval routing — Mode 1 only)
   - `D365 FO Provision Flow` (D365 F&O provisioning)
3. Open each flow → click **Edit** → verify all connection references are correctly bound
4. Save each flow after verifying connections

> 💡 If a flow shows **"Connection references not configured"**, click the connection reference
> field and select the appropriate existing connection, or create a new one.

---

## Phase 3: Configure the Agent in Copilot Studio

### Step 3-1. Open the Agent in Copilot Studio

1. Go to **Copilot Studio** → https://copilotstudio.microsoft.com
2. Switch to the correct Power Platform environment
3. Find and open **FOUserOnboardingAgent**

---

### Step 3-2. Review and Update Agent Instructions

1. In the agent editor, go to **Settings** → **AI capabilities**
2. Review the **Instructions (System Prompt)** — confirm the agent is configured to:
   - Only process D365 F&O user onboarding requests
   - Not use web search (disabled by default)
   - Collect the required fields: full name, email address, company, security roles
3. Update the fallback/escalation message if needed (e.g., contact IT helpdesk email address)

---

### Step 3-3. Review Onboarding Form Topics

1. Go to **Topics** in the left navigation
2. Open the **Onboarding Form** topic and confirm it collects:
   - Full name of the new hire
   - Email address (UPN)
   - D365 F&O company (legal entity code)
   - Security roles to assign
   - Requestor name and contact
3. Confirm the topic correctly calls the **FO User Onboarding Flow** via the action step

---

### Step 3-4. Test the Agent in Copilot Studio

1. Click **Test** in the top-right corner of Copilot Studio
2. Start a new conversation with the message: **"I need to onboard a new F&O user"**
3. Walk through the form:
   - Provide a test user name, email, company, and roles
4. Observe the flow execution in **Monitor** → **Flow runs** in Power Automate
5. Verify the expected behavior for your configured mode:
   - **Mode 1**: Confirm Teams Adaptive Card appears in the target channel
   - **Mode 2**: Confirm validation result (user found or "AD User not found" error)
   - **Mode 3**: Confirm direct provisioning confirmation

> 💡 Use a test user (e.g., `CAF.user@yourtenant.onmicrosoft.com`) to avoid affecting production data.

---

## Phase 4: Deploy to Microsoft Teams and M365 Copilot

### Step 4-1. Publish the Agent

1. In **Copilot Studio**, click **Publish** → **Publish**
2. Wait for the publishing process to complete (1–3 minutes)
3. Confirm the status shows **Published**

---

### Step 4-2. Add the Agent to Microsoft Teams

1. In **Copilot Studio**, go to **Channels** → **Microsoft Teams**
2. Click **Add to Teams**
3. In the dialog, click **Open in Teams**
4. In the Microsoft Teams app manifest page, click **Add** to install the agent for yourself
5. Test the agent by sending a message in the Teams chat

---

### Step 4-3. Deploy to M365 Copilot (Optional)

1. In **Copilot Studio**, go to **Channels** → **Microsoft 365 Copilot**
2. Click **Submit for admin approval**
3. In the **Microsoft 365 Admin Center** → **Integrated Apps**, find the submitted app
4. Click **Deploy** to make it available org-wide (or to a specific group)

> ⚠️ Org-wide deployment requires **Global Administrator** or **Application Administrator** approval
> in the Microsoft 365 Admin Center.

---

## Phase 5: Production Validation

After full deployment, run through the following validation checklist to confirm the agent
is working correctly end-to-end in production.

### Mode 1 Validation Checklist (CreateADUser=1, Approval=Yes)

| # | Validation Step | Expected Result | Status |
|---|---|---|---|
| 1 | Submit onboarding request via Teams agent | Form collects all required fields | ☐ |
| 2 | Graph API — user not found | Flow continues to user creation | ☐ |
| 3 | Graph API — user creation | `statusCode: 200`, `isusercreated: 1` returned | ☐ |
| 4 | Teams Adaptive Card | Card appears in `FNOUserApprove` channel within 60 seconds | ☐ |
| 5 | Approval action | Approver clicks Approve on Adaptive Card | ☐ |
| 6 | D365 F&O provisioning | User appears in D365 F&O with correct company and roles | ☐ |
| 7 | Confirmation message | Requestor receives success message in Teams | ☐ |

### Mode 2 Validation Checklist (CreateADUser=0)

| # | Validation Step | Expected Result | Status |
|---|---|---|---|
| 1 | Submit onboarding request for existing user | Flow validates user in Entra ID | ☐ |
| 2 | Path A — user found | Proceeds to D365 F&O provisioning | ☐ |
| 3 | Path A — D365 F&O provisioning | User provisioned successfully | ☐ |
| 4 | Path B — user not found | Error: "AD User not found. Cannot proceed." | ☐ |
| 5 | Path B — no provisioning | D365 F&O provisioning does NOT proceed | ☐ |

### Mode 3 Validation Checklist (CreateADUser=1, Approval=No)

| # | Validation Step | Expected Result | Status |
|---|---|---|---|
| 1 | Submit onboarding request — existing user | Flow detects existing user, shows skip notice | ☐ |
| 2 | Existing user — no Teams card | Adaptive Card is NOT sent to Teams | ☐ |
| 3 | Submit onboarding request — new user | Flow creates Entra ID user automatically | ☐ |
| 4 | New user creation | "AD User Created Successfully" message returned | ☐ |
| 5 | Direct provisioning | Both paths proceed to D365 F&O without approval step | ☐ |

---

## Troubleshooting Guide

### Issue: Teams Adaptive Card not appearing (Mode 1)

| Symptom | Likely Cause | Resolution |
|---|---|---|
| Card never appears in Teams channel | `TeamName` or `ChannelName` in `OnboardAgentConfig` is incorrect | Confirm values are exactly `F&O User Provisioning` and `FNOUserApprove` (case-sensitive — no trailing spaces) |
| Card never appears despite correct names | Flow uses Group ID / Channel ID and these are stale | Verify Group ID = `28b69a3d-b7af-4f5b-be42-a53bd17dd6c4` and Channel ID = `19%3A0oFu9NU3FTF6Dh1Y5CGCCOJx2yYl1XfCe5HAl-Gajko1%40thread.tacv2` in the flow action |
| Card appears but approval button fails | Teams connection in the approval flow is using wrong account | Re-authenticate the Teams connector in Power Automate with the service account that has access to `FNOUserApprove` |
| Flow times out waiting for approval | Approval card was not acted on within the flow timeout window | Increase the approval wait timeout in the Teams Approval Flow, or notify the `FNOUserApprove` channel approvers directly |

---

### Issue: Graph API returning 403 Forbidden

| Symptom | Likely Cause | Resolution |
|---|---|---|
| `403 Forbidden` on user lookup | Admin consent not granted for API permissions | Go to Azure Portal → App Registration → API permissions → Grant admin consent |
| `403 Forbidden` on user creation | `User.ReadWrite.All` or `Directory.ReadWrite.All` not granted | Add permissions and re-grant admin consent (Step 0-2) |

---

### Issue: D365 F&O provisioning fails

| Symptom | Likely Cause | Resolution |
|---|---|---|
| User not appearing in D365 F&O after flow | D365 F&O connection is using insufficient permissions | Confirm the service account used in the D365 F&O flow connection has **System Administrator** role in the target company |
| Security role not assigned | Role name does not match exactly | Verify the security role name in the onboarding form matches the internal D365 F&O role name (case-sensitive) |
| Company code not recognized | Legal entity code is incorrect | Confirm the company code (e.g., `USMF`, `GBSI`) matches the D365 F&O company's exact identifier |

---

### Issue: Agent not responding or showing error in chat

| Symptom | Likely Cause | Resolution |
|---|---|---|
| Agent gives generic error response | Flow connection references not configured | Open each flow in Power Automate → verify all connections are bound → turn flow off and on again |
| Agent starts but form does not complete | Topic action step is not correctly bound to the flow | In Copilot Studio → Topics → Onboarding Form → verify the action step calls the correct flow |
| Agent not available in Teams | Agent not published or Teams channel not configured | Republish in Copilot Studio → re-add to Teams channel |

---

## Related Resources

| Resource | Link |
|---|---|
| Scenario Overview | [1.Overview.md](./1.Overview.md) |
| Architecture | [2.Architecture.md](./2.Architecture.md) |
| Sample Prompts | [4.Sample-prompts.md](./4.Sample-prompts.md) |
| UAT Test Guide | [5.UAT-Test-Guide.md](./5.UAT-Test-Guide.md) |
| Microsoft Graph API — Create User | [Microsoft Learn](https://learn.microsoft.com/en-us/graph/api/user-post-users) |
| Microsoft Graph API — Get User | [Microsoft Learn](https://learn.microsoft.com/en-us/graph/api/user-get) |
| D365 F&O — Create Users | [Microsoft Learn](https://learn.microsoft.com/en-us/dynamics365/fin-ops-core/dev-itpro/sysadmin/tasks/create-new-users) |
| Power Automate — Teams Adaptive Cards | [Microsoft Learn](https://learn.microsoft.com/en-us/power-automate/create-adaptive-cards) |
| Copilot Studio Documentation | [Microsoft Learn](https://learn.microsoft.com/en-us/microsoft-copilot-studio/) |

---
